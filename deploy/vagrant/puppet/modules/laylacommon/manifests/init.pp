# Módulo 'laylacommon' — código compartido entre los manifiestos por VM.
# Carga de Puppet: deploy/puppet/modules/laylacommon/manifests/<nombre>.pp → laylacommon::<nombre>
class laylacommon {
  # Clase paraguas vacía — el contenido real vive en docker_install y stack.
}
