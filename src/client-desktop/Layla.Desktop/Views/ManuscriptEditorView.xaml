<Page x:Class="Layla.Desktop.Views.ManuscriptEditorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ManuscriptEditorView">

    <Grid Background="{DynamicResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="20,10" Background="{DynamicResource SidebarBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
            <TextBlock Text="Manuscript Editor (Local-First)" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}"/>
        </Border>

        <ToolBarTray Grid.Row="1" Margin="20,20,20,0" Background="{DynamicResource ControlBackground}">
            <ToolBar Background="Transparent">
                <Button Command="ApplicationCommands.Undo" Content="Undo"/>
                <Button Command="ApplicationCommands.Redo" Content="Redo"/>
                <Separator/>
                <Button Command="ApplicationCommands.Cut" Content="Cut"/>
                <Button Command="ApplicationCommands.Copy" Content="Copy"/>
                <Button Command="ApplicationCommands.Paste" Content="Paste"/>
                <Separator/>
                <Button Command="EditingCommands.ToggleBold" Content="Bold" FontWeight="Bold"/>
                <Button Command="EditingCommands.ToggleItalic" Content="Italic" FontStyle="Italic"/>
                <Button Command="EditingCommands.ToggleUnderline">
                    <TextBlock Text="Underline" TextDecorations="Underline"/>
                </Button>
                <Separator/>
                <Button Command="EditingCommands.IncreaseFontSize" Content="A+"/>
                <Button Command="EditingCommands.DecreaseFontSize" Content="A-"/>
                <Separator/>
                <Button Command="EditingCommands.AlignLeft" Content="Left"/>
                <Button Command="EditingCommands.AlignCenter" Content="Center"/>
                <Button Command="EditingCommands.AlignRight" Content="Right"/>
                <Button Command="EditingCommands.AlignJustify" Content="Justify"/>
                <Separator/>
                <Button Command="EditingCommands.ToggleBullets" Content="Bullets"/>
                <Button Command="EditingCommands.ToggleNumbering" Content="Numbering"/>
                <Separator/>
                <Button Command="EditingCommands.DecreaseIndentation" Content="Outdent"/>
                <Button Command="EditingCommands.IncreaseIndentation" Content="Indent"/>
                <Separator/>
                <Button x:Name="InsertImageButton" Content="Insert Image" Click="InsertImageButton_Click"/>
            </ToolBar>
        </ToolBarTray>

        <Border Grid.Row="2" Margin="20" Background="{DynamicResource ControlBackground}" CornerRadius="8" Padding="10">
            <RichTextBox x:Name="EditorRichTextBox" 
                         Background="Transparent"
                         Foreground="{DynamicResource PrimaryText}"
                         BorderThickness="0" 
                         FontSize="14" 
                         FontFamily="Segoe UI"
                         Padding="10"
                         VerticalAlignment="Top"
                         TextChanged="EditorRichTextBox_TextChanged"
                         PreviewMouseLeftButtonUp="EditorRichTextBox_PreviewMouseLeftButtonUp"
                         AcceptsReturn="True">
                <FlowDocument>
                    <Paragraph>
                        <Run Text="Start writing your amazing story here..."/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Border>

        <TextBlock x:Name="WordCountTextBlock" Grid.Row="3" Margin="20,0,20,20" 
                   HorizontalAlignment="Right" Foreground="{DynamicResource SecondaryText}" 
                   FontSize="12" FontFamily="Segoe UI" Text="{Binding WordCountText}"/>
    </Grid>
</Page>
