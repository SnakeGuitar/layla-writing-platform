<Page x:Class="Layla.Desktop.Views.WikiEntityEditorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="600"
      Title="WikiEntityEditorView" Background="{DynamicResource WindowBackground}">

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Create / Edit Wiki Entity" 
                   FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}"/>

        <!-- Form Fields -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Name:" VerticalAlignment="Center" FontWeight="Medium" Foreground="{DynamicResource SecondaryText}"/>
        <TextBox x:Name="NameTextBox" Grid.Row="2" Grid.Column="1" Height="30" Padding="5" 
                 Background="{DynamicResource ControlBackground}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Aliases:" VerticalAlignment="Center" FontWeight="Medium" Foreground="{DynamicResource SecondaryText}"/>
        <TextBox x:Name="AliasesTextBox" Grid.Row="4" Grid.Column="1" Height="30" Padding="5" 
                 Background="{DynamicResource ControlBackground}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" 
                 ToolTip="Separate aliases with commas"
                 Text="{Binding Aliases, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Description:" VerticalAlignment="Top" Margin="0,5,0,0" FontWeight="Medium" Foreground="{DynamicResource SecondaryText}"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="6" Grid.Column="1" Padding="5" 
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 Background="{DynamicResource ControlBackground}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="8" Grid.Column="0" Text="Tags:" VerticalAlignment="Center" FontWeight="Medium" Foreground="{DynamicResource SecondaryText}"/>
        <TextBox x:Name="TagsTextBox" Grid.Row="8" Grid.Column="1" Height="30" Padding="5" 
                 Background="{DynamicResource ControlBackground}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"
                 ToolTip="e.g. Fox, Character, Protagonist"
                 Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Action Area -->
        <StackPanel Grid.Row="10" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="100" Height="35" Margin="0,0,10,0" 
                    Background="{DynamicResource ControlBackground}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Cursor="Hand"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Save Entity" Width="120" Height="35" 
                    Background="{DynamicResource AccentColor}" Foreground="White" BorderThickness="0" Cursor="Hand"
                    FontWeight="SemiBold" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Page>
